# Optional: when set, the portal boots in connected (Cloud Agent) mode.
# Leave unset for offline-first / non-custodial Edge Agent mode.
# VITE_CLOUD_AGENT_API_ENDPOINT=https://your-cloud-agent.example.com/cloud-agent
